pipelines:

  - name: pipelines_test
    steps:
      - name: S_Bash_test
        type: Bash
        configuration:
          nodePool: azure-2384
        execution:
          onExecute:
            - echo "executing step..."
            - printenv
